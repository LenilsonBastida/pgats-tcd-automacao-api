{
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "chai": "^4.5.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mocha": "^11.7.2",
    "sinon": "^21.0.0",
    "supertest": "^7.1.4",
    "swagger-ui-express": "^5.0.1"
  },
  "name": "pgtas-tcd-automacao-api",
  "version": "1.0.0",
  "description": "Esta API permite registrar usuários, realizar login, cadastrar tarefas e consultar tarefas vinculadas ao usuário autenticado. O banco de dados é em memória, ideal para aprendizado de testes e automação de APIs.",
  "main": "app.js",
  "scripts": {
    "test": "mocha \"test/**/*.js\" --timeout 10000 --reporter mochawesome",
    "test-external": "mocha \"test/external/**/*.js\" --timeout 10000 --reporter mochawesome",
    "test-controller": "mocha \"test/controller/**/*.js\" --timeout 10000 --reporter mochawesome",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "mochawesome": "^7.1.4"
  }
}
